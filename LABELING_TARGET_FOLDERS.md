# 라벨링 대상 폴더 정리

## ✅ 정리 완료

**삭제된 항목:**
- ✅ `yolo_dataset/` (6.5MB) - 재생성 예정
- ✅ `runs/` (17MB) - 학습 결과
- ✅ `test_qt.py` - 테스트 파일
- ✅ `dataset/641447.txt` - 잘못 위치한 파일
- ✅ `dataset/classes.txt` - 불필요한 파일
- ✅ `__pycache__` 파일들

---

## 📁 라벨링 대상 폴더 (총 802개 이미지)

각 폴더의 이미지를 라벨링해야 합니다:

### 1. Center (100개)
```
dataset/Center/
  - 641447.jpg
  - 642989.jpg
  - ...
```

### 2. Donut (102개)
```
dataset/Donut/
  - 679360.jpg
  - 679825.jpg
  - ...
```

### 3. Edge Local (103개)
```
dataset/Edge Local/
  - 809657.jpg
  - 809662.jpg
  - ...
```

### 4. Edge Ring (102개)
```
dataset/Edge Ring/
  - ...
```

### 5. Local (100개)
```
dataset/Local/
  - ...
```

### 6. near full (95개)
```
dataset/near full/
  - ...
```

### 7. none (100개)
```
dataset/none/
  - ...
```

### 8. Scratch (100개)
```
dataset/Scratch/
  - ...
```

---

## 🎯 라벨링 순서

### Step 1: LabelImg 실행

```bash
./run_labeling.sh
# 또는
labelImg
```

### Step 2: 각 폴더별로 라벨링

1. **Donut 폴더부터** (도넛 이미지가 우선)
   - `Open Dir` → `dataset/Donut/` 선택
   - 각 이미지에서 불량 부분 박스 그리기
   - 클래스명: `defect` 입력
   - `D` 키로 다음 이미지

2. **나머지 폴더들 반복**
   - Local, Edge Local, Edge Ring, Scratch, near full, none, Center

### Step 3: 라벨 파일 확인

각 폴더에 `.txt` 파일이 생성되었는지 확인:
```
dataset/Donut/
  679360.jpg
  679360.txt  ← 라벨 파일
  679825.jpg
  679825.txt
  ...
```

---

## ✅ 라벨링 후 확인

```bash
python utils/check_labeling_status.py
```

---

## 📊 예상 소요 시간

- 각 이미지당 1-2분
- 802개 × 1-2분 = 약 **13-27시간**

**팁:** 
- 우선 각 패턴별로 10-20개씩만 라벨링하여 학습 테스트
- 결과가 좋으면 나머지 진행

