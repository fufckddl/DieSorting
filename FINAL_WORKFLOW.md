# 최종 워크플로우: 실제 라벨링 후 재학습

## 📋 작업 순서

### Step 1: 프로젝트 정리 (✅ 완료)

```bash
./cleanup_project.sh
```

정리 완료!

---

### Step 2: 라벨링 작업

#### 2-1. LabelImg 실행

```bash
./run_labeling.sh
```

#### 2-2. 각 패턴 폴더별로 라벨링

**라벨링 대상 폴더:**
- `dataset/Center/` (100개)
- `dataset/Donut/` (102개) ← 우선순위 높음 (도넛 이미지)
- `dataset/Edge Local/` (103개)
- `dataset/Edge Ring/` (102개)
- `dataset/Local/` (100개)
- `dataset/near full/` (95개)
- `dataset/none/` (100개)
- `dataset/Scratch/` (100개)

**라벨링 방법:**
1. `Open Dir` → 각 폴더 선택
2. `W` 키로 박스 그리기
3. 불량 부분 드래그
4. 클래스명: `defect` 입력
5. `D` 키로 다음 이미지

#### 2-3. 진행 상황 확인

```bash
python utils/check_labeling_status.py
```

---

### Step 3: YOLO 데이터셋 재생성

라벨링 완료 후:

```bash
python utils/convert_labels_to_yolo.py organize \
    --image-dir dataset/ \
    --labels-dir dataset/ \
    --output-dir yolo_dataset/
```

이 명령이:
- 모든 패턴 폴더에서 이미지와 라벨 찾기
- Train/Val 자동 분할 (80:20)
- YOLO 학습용 데이터셋 구조 생성

---

### Step 4: YOLO 재학습

```bash
python train_yolo.py --data yolo_dataset/dataset.yaml --epochs 100
```

---

### Step 5: 결과 확인

```bash
python main.py
```

GUI에서:
1. 이미지 선택
2. "분석 시작" 클릭
3. **정확한 위치에 박스 표시 확인** ✅

---

## ⏱️ 예상 시간

- 라벨링: 13-27시간 (802개)
- 학습: 5-10시간 (CPU 기준)

**시간 절약 팁:**
- 각 패턴별로 20개씩만 먼저 라벨링
- 학습 테스트 후 결과 확인
- 문제없으면 나머지 진행

---

## 📊 현재 상태

- ✅ 프로젝트 정리 완료
- ⏳ 라벨링 대기 중 (0/802)
- ⏳ 데이터셋 재생성 대기
- ⏳ 재학습 대기

---

## 🎯 다음 단계

**지금 바로 시작:**

```bash
./run_labeling.sh
```

`dataset/Donut/` 폴더부터 라벨링을 시작하세요!


